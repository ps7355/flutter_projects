import 'package:flutter/material.dart';

class Sum extends StatelessWidget {
  const Sum({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Sum Page")),
    );
  }
}
